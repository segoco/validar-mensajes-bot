{
  "name": "validar-mensajes-bot",
  "version": "1.0.0",
  "description": "validar mensajes de bot en su formato",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "@segoco",
  "license": "ISC",
  "dependencies": {
    "chalk": "^5.3.0",
    "nodemon": "^3.1.0"
  }
}
